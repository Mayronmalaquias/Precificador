---
tags: [processo/inteligencia, nivel/2, dominio/bi, status/frontend-pronto]
complexidade: alta
dono: Diretoria (consome) · Inteligência (mantém)
frequencia: consulta contínua
sistemas: [Frontend React, gerente-dashboard, jornada de captação, vendas, C2S, DFImóveis]
status_evolucao: frontend e endpoint executivo implementados · deploy pendente
origem_doc: implementação da página VisaoDiretor em 2026-08-05
---

# 2.14 — Visão do Diretor / Executive View

## 1. Objetivo

Disponibilizar à diretoria uma visão executiva única para acompanhamento da performance dos
gerentes e de suas equipes, reunindo indicadores comerciais, jornada de captação, vendas,
eficiência de mídia e pendências operacionais.

A interface prioriza leitura rápida (*scannability*), comparação entre equipes e identificação
imediata de situações que exigem ação da diretoria.

Rota do frontend:

```text
/VisaoDiretor
```

## 2. Acesso e permissionamento

A página é exclusiva para usuários cuja `permissao` seja exatamente `diretor`.

Foram implementadas duas camadas no frontend:

1. O item **Visão do Diretor** só aparece no menu quando `permissao === "diretor"`.
2. A rota utiliza `DiretorRoute`, que valida `isLogado` e `isDiretor` antes de renderizar a tela.

Comportamento da guarda:

- usuário não autenticado → redirecionado para `/login`;
- usuário autenticado sem permissão de diretor → redirecionado para `/`;
- diretor autenticado → acesso liberado.

> A API deverá repetir a validação de permissão nos endpoints executivos. A proteção do frontend
> melhora a experiência, mas não substitui autorização no backend.

## 3. Arquivos criados e alterados

### Novos arquivos

| Arquivo | Responsabilidade |
|---|---|
| `frontend/src/components/VisaoDiretor.js` | Estrutura, estado, filtros, gráficos, tabelas e modais da página |
| `frontend/src/assets/css/VisaoDiretor.css` | Identidade visual, grids, gráficos CSS/SVG e responsividade |
| `frontend/src/auth/DiretorRoute.js` | Guarda de rota exclusiva da diretoria |

### Arquivos alterados

| Arquivo | Alteração |
|---|---|
| `frontend/src/App.js` | Registro da rota `/VisaoDiretor` protegida por `DiretorRoute` |
| `frontend/src/components/Header.js` | Inclusão do item de menu visível somente para diretores |

## 4. Estrutura da página

### 4.1 Cabeçalho e filtros globais

O topo apresenta:

- título **Visão do Diretor** e contexto executivo;
- indicador visual de atualização dos dados;
- períodos rápidos: esta semana, este mês e trimestre;
- período personalizado com data inicial e final;
- filtro por equipe e gerente.

O identificador de escopo é o `team` (`equipes.id_equipe`, como `G61017`), e não o
`id_usuarios` do gerente.

### 4.2 KPIs executivos

Foram criados quatro cards:

- total de vendas em reais;
- propostas originadas de visitas, explicitamente descritas como `SIM`, `NÃO` e `TALVEZ`;
- visitas realizadas;
- leads captados por C2S e DFImóveis.

Cada card suporta valor principal, contexto auxiliar e variação positiva ou negativa em relação
ao período anterior.

### 4.3 Performance de equipes e gerentes

#### Visitas por equipe

Gráfico horizontal comparando realizado e meta por equipe. Também apresenta o total consolidado
e o percentual de cumprimento da meta.

#### Busca e captação

Exibe imóveis trabalhados na prospecção, novas captações, variação do período e conversão. Há
uma linha de tendência em SVG para representar a evolução temporal.

#### Movimentações na jornada

Lista quantos imóveis avançaram para cada etapa durante o intervalo selecionado. A implementação
visual prevê etapas de avaliação, prospecção, apresentação e captação.

#### Funil de propostas

O consolidado usa três grupos:

- aceitas — `SIM`;
- em negociação — `TALVEZ`;
- recusadas — `NÃO`.

O módulo combina gráfico circular consolidado, legenda numérica e barras empilhadas por equipe.

### 4.4 Vendas recentes

Tabela com:

- imóvel e código;
- equipe e gerente responsável;
- valor de fechamento;
- data;
- situação do contrato ou comissão.

Ao selecionar uma linha, a página abre um modal de detalhamento. O modal está preparado para
receber histórico, responsáveis, comentários e ações quando o endpoint de detalhe for ligado.

### 4.5 Gestão de mídia — DFImóveis e C2S

O módulo demonstra o cruzamento:

```text
Impressões → Acessos → Leads gerados
```

Inclui:

- totais e percentuais de conversão entre etapas;
- identificação da atualização da planilha XLSX;
- linha comparativa de impressões e acessos;
- desempenho por bairro e perfil de imóvel;
- filtro de bairro;
- tipologia e faixa de valor na descrição do perfil;
- variação de performance por segmento.

Os gráficos foram construídos em SVG e CSS nativos, sem adicionar bibliotecas ou dependências ao
projeto.

### 4.6 Auditoria e pendências

Painel escuro de alto contraste destinado a situações que exigem atenção. A demonstração contempla:

- lead recebido e ainda não visualizado;
- atividade atrasada na jornada de captação;
- imóvel sem atualização;
- revisão de visita pendente.

Cada ocorrência apresenta equipe/gerente, descrição e tempo de atraso. O seletor **Somente
críticos** reduz a lista às ocorrências de maior severidade. A seleção de um alerta também abre
o modal de detalhes.

As flags existentes de visita que devem alimentar esse painel são:

- `viu_anexo`;
- `viu_notas`;
- `add_motivo`.

### 4.7 Auditoria das visitas por equipe

A página apresenta uma linha por equipe/gerente com quatro quantidades pendentes:

- **viu a visita** — existe registro da visita em `gerente_visita_visualizada`;
- **viu a nota** — flag `viu_notas`;
- **viu o anexo** — flag `viu_anexo`;
- **adicionou motivo** — flag `add_motivo`, usada para propostas `SIM` ou `TALVEZ`.

A consulta parte de `visitas` e usa `LEFT JOIN` com as flags. Assim, visitas que nunca foram
abertas pelo gerente também aparecem no relatório, com todas as confirmações pendentes. Nota,
anexo ou motivo são marcados como “não se aplica” quando a própria visita não possui aquele
conteúdo ou quando a proposta não é `SIM/TALVEZ`.

As contagens de nota e anexo consideram somente visitas que possuem esses conteúdos. A pendência
de motivo considera somente propostas `SIM` ou `TALVEZ`. O módulo respeita os mesmos filtros
globais de período e equipe e ordena primeiro as equipes com mais pendências.

## 5. Estado atual dos dados

A massa demonstrativa foi integralmente removida. A tela consome o endpoint real
`GET /api/v1/diretor-dashboard/executivo`, que valida o usuário solicitante como diretor ativo e
consolida equipes, visitas, propostas, contratos, captações, leads e pendências diretamente do banco.

Funcionam com dados reais:

- seleção de período;
- seleção de equipe;
- recálculo do comparativo de visitas;
- recálculo do funil de propostas;
- filtro de bairro/perfil de mídia;
- filtro de alertas críticos;
- abertura e fechamento de modais;
- adaptação para desktop, tablet e celular.

O relatório `legado/Relatorio-de-acesso-imoveis-04_08_2026 17_02_25.xlsx` é a fonte atual do
módulo DFImóveis. A API escolhe o relatório `Relatorio-de-acesso-imoveis-*.xlsx` mais recente,
valida as colunas e agrega impressões, acessos e interações por bairro.

No XLSX, “interações de lead” é a soma de `Emails`, `Telefone`, `WhatsAppEmailsGerados`, `Visita`
e `Proposta`. Tipologia, faixa de valor e metragem não são exibidas porque não existem no arquivo.

## 6. Contrato de API sugerido

Para evitar múltiplas requisições e diferenças de período entre módulos, recomenda-se um endpoint
consolidado:

```http
GET /api/v1/diretor-dashboard/executivo
    ?solicitante_id=C61xxx
    ?periodo=mes
    &data_inicio=2026-08-01
    &data_fim=2026-08-31
    &team=G61017
```

Parâmetros:

| Parâmetro | Uso |
|---|---|
| `solicitante_id` | ID do diretor ativo; obrigatório para autorização |
| `periodo` | Atalho temporal: semana, mês, trimestre ou personalizado |
| `data_inicio` | Início inclusivo do intervalo |
| `data_fim` | Fim inclusivo do intervalo |
| `team` | Equipe selecionada; ausente significa todas as equipes |

Estrutura de resposta sugerida:

```json
{
  "atualizado_em": "2026-08-05T14:30:00-03:00",
  "periodo": {
    "inicio": "2026-08-01",
    "fim": "2026-08-31"
  },
  "kpis": {
    "vendas": { "valor": 8540000, "variacao_pct": 12.8 },
    "propostas": { "valor": 192, "variacao_pct": 8.4 },
    "visitas": { "valor": 153, "variacao_pct": 5.7 },
    "leads": { "valor": 338, "variacao_pct": -2.1 }
  },
  "equipes": [],
  "jornada": [],
  "propostas": {
    "sim": 42,
    "nao": 65,
    "talvez": 46,
    "por_equipe": []
  },
  "vendas_recentes": [],
  "midia": {
    "totais": {},
    "serie": [],
    "perfis": []
  },
  "pendencias": []
}
```

## 7. Fontes de dados previstas

| Módulo | Fonte prevista |
|---|---|
| Visitas | Domínio de visitas e endpoints de `gerente-dashboard` |
| Propostas | Campo de proposta das visitas, normalizado para `SIM`, `NÃO` ou `TALVEZ` |
| Equipes | `equipes.id_equipe` e relacionamento por `usuarios.team` |
| Jornada | `captacao` e `captacao_historico` |
| Vendas | Domínio de vendas e contratos |
| Impressões e acessos | Upload semanal XLSX do DFImóveis |
| Leads | C2S e DFImóveis, com regra de deduplicação a definir |
| Revisão de visitas | `gerente_visita_visualizada` e suas flags |
| Atividades atrasadas | Datas previstas e realizadas da jornada de captação |
| Imóveis desatualizados | Última alteração relevante do imóvel/captação |

## 8. Regras de cálculo a confirmar

Antes da implementação do backend, a diretoria deve validar:

1. O que caracteriza uma venda no período: data do fechamento, assinatura ou pagamento.
2. Se o total de propostas conta todas as visitas ou somente a última resposta por cliente/imóvel.
3. Qual é a fonte oficial da meta de visitas de cada equipe.
4. O que representa “volume de busca”: imóveis consultados, contatos feitos ou entradas na jornada.
5. Quais mudanças de etapa contam como avanço e como tratar retornos de etapa.
6. Como deduplicar leads iguais entre C2S e DFImóveis.
7. Qual SLA define lead não visualizado, atividade atrasada e imóvel desatualizado.
8. Se administradores podem acessar a página ou se ela continuará exclusiva para diretores.

## 9. Responsividade e acessibilidade

- Desktop: KPIs em quatro colunas e módulos principais em duas colunas.
- Tablet: KPIs em duas colunas e módulos analíticos empilhados.
- Celular: coluna única, tabelas com rolagem horizontal e filtros reorganizados.
- Botões e linhas interativas usam elementos semânticos.
- O modal utiliza `role="dialog"`, `aria-modal` e título acessível.
- Alertas não dependem apenas de cor: também exibem texto, tipo e tempo de atraso.

## 10. Validação realizada

O frontend foi compilado com:

```powershell
cd frontend
npm.cmd run build
```

Resultado: build de produção concluído com sucesso. Os avisos do ESLint apresentados durante a
compilação pertencem a componentes preexistentes e não foram introduzidos por `VisaoDiretor`.

## 11. Próximas etapas

1. Validar visual e regras de cálculo com a diretoria.
2. Criar fluxo de upload semanal do XLSX de mídia, substituindo a leitura do arquivo no repositório.
3. Definir retenção histórica dos relatórios DFImóveis para séries temporais.
4. Complementar o arquivo de mídia com tipologia, faixa de valor e metragem, se o portal permitir.
5. Definir deduplicação e atribuição dos leads C2S/DFImóveis.
6. Implementar SLAs configuráveis para pendências.
7. Criar testes de permissão, filtros e consistência dos totais.

## 12. Atualizações da implementação — 2026-08-05

### 12.1 Remoção dos dados demonstrativos

Todas as equipes, gerentes e métricas fictícias foram removidos do componente. A página passou a
consumir exclusivamente `GET /api/v1/diretor-dashboard/executivo`, que consolida os dados reais
do sistema.

Quando uma fonte ou dimensão não existe, a interface mostra ausência de dados ou uma explicação da
limitação. Nenhum valor é estimado para preencher o layout.

### 12.2 Vendas recentes

O módulo **Vendas recentes** possui duas regras distintas:

- o KPI de vendas respeita o período e a equipe selecionados;
- a tabela busca os oito contratos mais recentes cadastrados, independentemente do período, para
  não ficar vazia quando não houver fechamento no intervalo atual.

A seleção de equipe continua restringindo os contratos pelo gerente de venda ou captação. A coluna
de status de contrato/comissão foi substituída por **Valor Total 61**, lido de
`contratos.valor_total_61`.

### 12.3 Funil de visitas

O antigo nome **Funil de propostas** foi substituído por **Funil de visitas**. O número central
representa a quantidade de visitas que possuem uma proposta classificada.

As categorias exibidas são:

- Proposta `SIM`;
- Proposta `TALVEZ`;
- Proposta `NÃO`.

Visitas sem uma dessas classificações não entram no total do funil.

### 12.4 Relatório DFImóveis

O arquivo real analisado possui uma aba `Relatório`, 757 imóveis e as colunas:

```text
Endereco · CodigoDeBusca · Negocio · SituacaoDoCadastro · Acesso · Emails · Telefone
WhatsAppEmailsGerados · Indique · IndiqueWhatsapp · Impressao · Termo
CompartilheFacebook · CompartilheGoogle · CompartilheTwitter
AtendimentoOnlineParaLancamento · Visita · Proposta
```

A conversão é apresentada na ordem definida pela diretoria:

```text
Acessos → Impressões → Interações de lead
```

As interações de lead correspondem à soma de:

```text
Emails + Telefone + WhatsAppEmailsGerados + Visita + Proposta
```

O backend localiza o arquivo mais recente cujo nome segue
`Relatorio-de-acesso-imoveis-*.xlsx`. A data do relatório é extraída do nome do arquivo.

#### Agrupamento por bairro

O bairro é extraído do campo `Endereco`, respeitando os formatos encontrados no relatório:

- `DF - BRASILIA - ASA SUL - ...` → `Asa Sul`;
- `DF - AGUAS CLARAS - NORTE - ...` → `Águas Claras`;
- `GO - LUZIANIA - VILA JURACY - ...` → `Luziânia`.

A lista **Onde a mídia converte melhor** apresenta o nome real do bairro como título, seguido de
acessos, impressões e interações. O texto do bairro possui cor própria para não herdar a cor branca
dos estilos globais de botões.

O arquivo não possui tipologia, faixa de valor ou metragem. Essas dimensões não são inferidas.

### 12.5 Movimentações na jornada

O histórico de captação grava avanços com `tipo = "avanco"`. Em várias movimentações automáticas,
`captacao_historico.data_acao` permanece nulo e a data real está em `created_at`.

A consulta executiva passou a usar:

```text
COALESCE(captacao_historico.data_acao, DATE(captacao_historico.created_at))
```

Isso permite contar corretamente os imóveis que avançaram para Prospecção, Interação,
Apresentação e Captação durante o período selecionado.

### 12.6 Auditoria das flags por equipe

A primeira versão mostrava uma linha para cada visita. O módulo foi reorganizado para uma visão
executiva por equipe/gerente, exibindo:

- quantidade de visitas não visualizadas;
- quantidade de notas não visualizadas;
- quantidade de anexos não visualizados;
- quantidade de motivos `SIM/TALVEZ` não adicionados;
- total de pendências da equipe.

A consulta parte de todas as visitas e faz junção externa com `gerente_visita_visualizada`.
Consequentemente, uma visita que nunca foi aberta também aparece como não visualizada.

As contagens aplicam as seguintes regras:

- nota pendente somente quando a visita possui nota ou áudio;
- anexo pendente somente quando a visita possui ficha, imagem ou anexo;
- motivo pendente somente quando a proposta é `SIM` ou `TALVEZ`;
- equipes com mais pendências aparecem primeiro;
- equipe sem pendências recebe o estado **Tudo revisado**.

### 12.7 Cabeçalho e ajustes de layout

O cabeçalho inicial herdava estilos globais aplicados à tag HTML `<header>`, produzindo um bloco
rosa incompatível com o restante do dashboard. A marcação foi alterada para uma seção com classe
exclusiva e recebeu:

- superfície clara com gradiente discreto;
- borda e sombra suaves;
- hierarquia tipográfica própria;
- indicador de atualização dos dados;
- comportamento responsivo independente.

O grid do módulo de mídia também foi configurado com `align-items: start`. Assim, o card de
conversão mantém a altura do conteúdo e não cria um grande espaço branco para acompanhar a altura
da lista de bairros ao lado.

Os estilos complementares estão em:

```text
frontend/src/assets/css/VisaoDiretorPolish.css
```

### 12.8 Otimização de carregamento

Foram aplicadas três camadas de otimização:

#### Consultas consolidadas

O backend deixou de consultar usuários e visitas separadamente para cada equipe. Visitas, equipes
e propostas são obtidas em uma consulta conjunta e agregadas em memória por `usuarios.team`.

Isso reduz o padrão anterior de múltiplas consultas por equipe e por período.

#### Cache do XLSX

A leitura do relatório DFImóveis utiliza cache em memória baseado em:

```text
caminho absoluto + data de modificação do arquivo
```

Enquanto o XLSX não mudar, o Pandas não relê nem reprocessa as 757 linhas. Uma nova versão do
arquivo invalida o cache automaticamente pela alteração de `st_mtime_ns`.

#### Cache da visão executiva

`executive_view()` utiliza cache de 180 segundos por combinação de argumentos:

```text
data inicial + data final + equipe
```

No frontend, a última resposta real de cada combinação também é armazenada em `sessionStorage`.
Ao retornar à página ou a um filtro já consultado, a interface mostra imediatamente essa resposta
e atualiza os dados em segundo plano.

O cache de sessão não contém dados demonstrativos e é descartado ao encerrar a sessão do navegador.

### 12.9 Validações após as alterações

Backend:

```powershell
python -m py_compile backend/app/services/diretor_dashboard_service.py
```

Frontend:

```powershell
cd frontend
npm.cmd run build
```

Ambas as validações foram concluídas com sucesso. Os avisos de ESLint apresentados pertencem a
componentes preexistentes, principalmente `Formulario`, `JornadaCaptacao`, `NovaVisita` e
`RelatorioGerente`.

### 12.10 Persistência e atualização do XLSX DFImóveis

Os relatórios deixaram de depender do arquivo presente no repositório. Foi criada a tabela
`dfimoveis_acessos`, versionada pela migration `20260805_dfimoveis_acessos`.

Cada linha armazena:

- data e nome do relatório importado;
- responsável pelo upload;
- endereço, bairro, código, negócio e situação cadastral;
- acessos e impressões;
- e-mail, telefone, WhatsApp, visita e proposta;
- demais interações existentes no XLSX;
- data da importação.

A chave única é `data_relatorio + codigo_busca`. Um novo upload para uma data já existente
substitui atomicamente somente aquele snapshot. Relatórios de outras datas são preservados para
evolução histórica.

Em **Gestão de Bases → Importar**, o card **Acessos DFImóveis** aceita exclusivamente `.xlsx` e
permite informar a data do relatório. Se a data não for informada, o backend tenta extraí-la do
nome `DD_MM_AAAA`; na ausência desse padrão, utiliza a data do upload.

Endpoint:

```http
POST /api/v1/admin/bases/importar/dfimoveis-acessos
Content-Type: multipart/form-data

arquivo=<xlsx>
data_relatorio=AAAA-MM-DD
criado_por=C61xxx
```

Após uma importação bem-sucedida, os caches executivos são invalidados. A Visão do Diretor consulta
o snapshot mais recente diretamente do banco. A leitura do arquivo no repositório permanece apenas
como compatibilidade transitória para ambientes que ainda não aplicaram a migration.

Em 05/08/2026, a migration foi aplicada ao RDS e o arquivo
`Relatorio-de-acesso-imoveis-04_08_2026 17_02_25.xlsx` foi importado:

```text
data do relatório: 04/08/2026
linhas inseridas: 757
duplicados: 0
```

## Atualização — Estoque fidedigno (2026-08-12)

O card **"Captações, saídas e estoque"** (antes "Entradas, saídas e estoque") saiu das planilhas
semanais e passou a ser calculado a partir da **situação do imóvel no Imoview**. As três leituras
foram fechadas com a diretoria:

| Número | Definição | Fonte |
|---|---|---|
| **Estoque** | imóveis com situação *Vago/Disponível* **agora** | `imovel_area` (catálogo) |
| **Captações** | imóveis registrados no sistema **no período** | `fato_captacao.data_entrada` |
| **Saídas** | imóveis que **deixaram** de estar disponíveis no período | `imovel_situacao_evento` |

**Estoque é saldo, não acumulado** — não tem recorte de período. Filtrar "julho" muda captações e
saídas; o estoque continua sendo o de hoje.

### Por que saída exigiu uma tabela nova

Saída é uma **transição** ("estava disponível, deixou de estar"), e `imovel_area.situacao` guarda
só o estado atual. A API do Imoview não tem endpoint de movimentação. A mudança só existe se
alguém comparar duas varreduras — é o que o `sync_areas_imoview.py` faz agora, gravando uma linha
em `imovel_situacao_evento` a cada transição detectada.

Conta como saída: **vendido, desativado, em reforma**.
**Em moderação NÃO conta** — o imóvel continua sendo nosso, o corretor só está ajustando o
anúncio. Por isso a varredura passou a incluir as situações 4 e 5 (4 + 81 imóveis, ~5 páginas):
sem elas, um imóvel em moderação sumiria da varredura e seria deduzido como desativado, entrando
como saída indevida.

Imóvel **desativado** (situação 6, 9.312 registros) é deduzido por **ausência**: baixar 466
páginas todo dia só para confirmar o óbvio não se paga.

### 🔴 O `quantidade` da API mente

O total de estoque vinha do campo `quantidade` da primeira página de `RetornarImoveis` com
`situacao=1`. Auditoria de 12/08/2026, percorrendo as 53 páginas:

```text
quantidade declarada pela API : 853
códigos únicos ao percorrer   : 1.051   (todos com situação "Vago/Disponível")
```

**O painel vinha subnotificando ~200 imóveis (-19%).** O número certo é o do catálogo, que é
montado percorrendo as páginas. Mesmo padrão nas outras situações: vendidos 1.175 declarados
contra 1.294 reais.

### Primeira execução é baseline

A tabela de eventos vazia significa "ainda não sei o que mudou" — as diferenças acumuladas não
são do dia. Nessa execução o job reconcilia as situações **sem gravar evento** e avisa no log
(`[BASELINE: eventos NAO gravados]`). O histórico de saídas começa da 2ª varredura.

> 🐛 **Armadilha do baseline (corrigida em 2026-08-13).** "Tabela vazia" continua verdadeiro
> *depois* do baseline — que por definição não escreve nada. Resultado: toda execução seria
> baseline e **nenhum evento sairia jamais**. Detectado quando a 2ª varredura logou
> `[BASELINE]` de novo. O baseline agora grava uma linha-marco (`codigo='__baseline__'`),
> datada de **amanhã** porque as transições do próprio dia não foram capturadas. Ela nunca
> entra na contagem de saídas: o filtro exige `situacao_anterior` começando em "vago".

Enquanto o log não cobre o período consultado, as saídas caem na planilha semanal (`fato_saida`).
As duas fontes são separadas **por data**, nunca sobrepostas: até a véspera do primeiro evento
vale a planilha, dali em diante vale o log, e um período que cruza a virada soma os dois pedaços.
O campo `saidas_fonte` diz o que foi usado (`planilha`, `situacao` ou `planilha+situacao`).

### Limite do filtro por equipe

Nem o catálogo nem o log sabem de quem é o imóvel — a única ponte entre imóvel e equipe é
`fato_captacao.captador1`. Medido em 12/08/2026:

```text
estoque total          : 1.052
com captação registrada:   825   (78%)
```

Ou seja: **a soma dos estoques por equipe não fecha com o total da empresa**, e não deve mesmo.
Os 227 imóveis sem captação registrada só aparecem na visão global. A tela avisa isso quando há
filtro ativo.


## Atualização — Cards financeiros (2026-08-13)

Três KPIs novos ao lado de VGV/VGC: **Valor comissão**, **NF 61** e **Líquido 61**. Todos saem
de colunas do contrato (`contratos`), a mesma fonte do VGV/VGC — não da planilha legada, que
parou em 18/06.

São **degraus da mesma comissão**, cada um um passo a menos:

| Card | Coluna | O que é |
|---|---|---|
| VGV | `valor_negocio` | valor do imóvel vendido |
| **Valor comissão** | `valor_comissao` | comissão total do negócio, **incluindo a parte de parceiros** |
| VGC | `valor_total_61` | o que fica com a 61 |
| **NF 61** | `nf_61_imoveis` | o que foi faturado |
| **Líquido 61** | `liquido_61` | o que sobra depois dos impostos |

A ordem foi verificada, não suposta: em **1.580 de 1.580** contratos com os campos preenchidos,
`valor_comissao ≥ valor_total_61 ≥ nf_61_imoveis ≥ liquido_61`. **Zero exceções.**

Julho/2026, empresa: VGV 23.598.315 · comissão 893.774 · VGC 715.042 · NF 325.777 ·
líquido 272.578. Respeitam o filtro de equipe e de corretor como os demais (SENNA: 4.623.315 ·
168.999 · 121.950 · 54.557 · 45.648).

Preenchimento na base: 1.581 contratos têm `valor_comissao`, 1.571 têm NF e líquido, de 1.591
no total — os poucos vazios entram como zero na soma.

## Atualização — Teto das barras e funil de conversão (2026-08-13)

### Barras de execução comercial: teto fixo em 150

As barras (leads · clientes · visitas por equipe/corretor) escalavam pelo **líder da
métrica**. Efeito ruim: a barra cheia significava um número diferente a cada filtro e a cada
período — trocar o recorte "aumentava" a barra de quem não mudou de número, e comparar duas
telas lado a lado não dizia nada.

Agora a escala é fixa: **barra cheia = 150**. Quem passa disso enche a barra e recebe uma
listra diagonal (`.ev-bar-track b.estourou`) — sem a marca, três equipes acima do teto
pareceriam empatadas. O número exato continua ao lado da barra.

### Funil de conversão

Card novo, em linha própria: **leads → clientes → visitas → propostas → vendas**, com a
conversão **entre etapas vizinhas** e o total lead→venda no rodapé.

Seletor de recorte com três opções — **61** (empresa), **Gerente** e **Corretor**. Escolhendo
gerente ou corretor, um segundo dropdown lista quem teve movimento no período (ordenado por
visitas). Os três recortes vêm **no mesmo payload** (`data.funil`): são poucos números e a
alternativa seria uma ida ao servidor a cada clique.

Agosto/2026 até o dia 13, empresa: 475 leads · 109 clientes · 154 visitas · 6 propostas ·
7 vendas.

> **A soma das linhas não fecha com o 61, e isso é esperado.** Cada etapa tem uma amarração
> diferente com a pessoa:
>
> | Etapa | Amarração | O que escapa |
> |---|---|---|
> | Leads | `leads_legado.atendimento` (id **ou** nome) | lead sem atendimento definido conta no 61 e em ninguém |
> | Clientes / Visitas | `visitas.id_corretor` | — |
> | Propostas | a proposta pertence ao **gerente** | corretor só aparece se ele for o gerente |
> | Vendas | nome no contrato | corretor renomeado sem alias em `pessoa_alias` |
>
> Na venda há um segundo passe: sem corretor identificado, a venda ainda é atribuída à equipe
> pelo nome do gerente do contrato. Por isso o total por equipe é mais confiável que o total
> por corretor.

Conversão só aparece quando há base: **0 leads não vira 0%**, vira "—". Zero e "não dá para
calcular" são coisas diferentes.

## Atualização — Qualidade atendimento (2026-08-13)

O card "Qualidade das visitas / Visitas" virou **Qualidade atendimento**. Duas mudanças
dentro dele:

### Barra empilhada: número de cada parte

Antes só o **total** aparecia ao lado da barra. Com isso, 40 visitas com 2 SIM e 40 visitas
com 30 SIM desenhavam barras do mesmo tamanho e mostravam o mesmo número — a composição, que
é o ponto do card, ficava só na proporção colorida. Agora cada parte traz o próprio número,
em selo da cor correspondente (SIM verde · TALVEZ âmbar · NÃO cinza), com o total ao lado.

### Clientes por recorrência

Bloco novo no rodapé do card: dos clientes que **visitaram no período**, quantos são de
primeira visita e quantos são recorrentes.

| Faixa | Julho/2026 (empresa) | % |
|---|---|---|
| 1 visita | 174 | 55,4% |
| 2 visitas | 61 | 19,4% |
| 3-5 | 63 | 20,1% |
| 6-7 | 10 | 3,2% |
| 8-10 | 3 | 1,0% |
| 10+ | 3 | 1,0% |

**314 clientes, 679 visitas no histórico deles.** Mais da metade está na primeira visita.

Duas contagens diferentes, de propósito (`_clientes_por_frequencia`):

- **quem entra na conta** = cliente com visita **dentro do período**, respeitando o filtro de
  equipe/corretor como o resto do painel;
- **em que faixa ele cai** = o **histórico inteiro** dele, sem recorte de data nem de equipe.
  Cortar pelo período transformaria todo mundo em cliente de 1 visita e o bloco não
  responderia nada.

> As faixas são **disjuntas**: `10+` é **estritamente acima de 10**, senão um cliente com
> exatamente 10 visitas cairia em duas faixas e a soma passaria de 100%. Conferido: as faixas
> somam 314 = o total. O percentual pode fechar em 100,1% por arredondamento de 1 casa.

## Atualização — Estoque e governança (2026-08-13)

### Saída de estoque: a definição mudou de fonte

Estava vindo **vazia**. A causa era o desenho anterior: saída lia o log de transições
(`imovel_situacao_evento`), que só enxerga do dia da instalação em diante — qualquer período
anterior dava zero, e o fallback caía numa planilha semanal desatualizada.

A regra correta já estava disponível no próprio payload do Imoview:

```text
saída = datahoraultimasituacao dentro do período
        E situação atual ≠ Vago/Disponível
        E situação atual ≠ Em moderação
```

`datahoraultimasituacao` vem preenchido em **100%** dos imóveis, então o número vale para
trás. Gravado em `imovel_area.situacao_em`. Resultado imediato:

| Período | Saídas (antes) | Saídas (agora) |
|---|---|---|
| Junho/2026 | 0 | **15** |
| Julho/2026 | 0 | **11** |
| Agosto/2026 (até 13) | 0 | **6** |

O log de transições continua sendo gravado pelo sync, mas o painel **não lê mais dele** —
serve como histórico de auditoria de quem mudou para quê.

> **Ressalva do desativado.** Situação 6 (9.312 imóveis) não é varrida — custaria ~466
> páginas por dia. Imóvel desativado continua sendo deduzido por **ausência** na varredura e
> recebe a **data da detecção**, não a data real da mudança. Vendido e em reforma, que são
> varridos, trazem a data verdadeira do Imoview.

### Locação fora do estoque

`imovel_area.finalidade` (Venda | Aluguel) passou a ser gravada. Locação é outra operação e
saiu do estoque, das saídas e das captações. Distribuição atual do catálogo: **2.372 venda,
77 aluguel, 16 sem finalidade** (registros anteriores à coluna, que continuam entrando).

Efeito no número: **estoque foi de 1.052 para 1.004** disponíveis.

Nas captações o filtro é indireto — `fato_captacao` não sabe a finalidade, então são
excluídos os códigos que o catálogo marca como aluguel.

### Recorte por gerente/corretor nos dois blocos

**Estoque** e **Governança** ganharam o mesmo seletor do funil: chips **61 · Gerente ·
Corretor** com dropdown de quem tem movimento. Julho: 7 gerentes e 47 corretores com estoque.

No bloco de governança o recorte é resolvido **na tela**: cada item de `imoveis_parados` passou
a carregar `team`, `equipe` e `id_corretor`, e os cards de faixa, o cruzamento e a paginação
são todos contados sobre a base recortada. Sem isso o card mostraria o número da empresa
enquanto a lista mostraria o do corretor.

### Cruzamento etapa × faixa

O bloco já dizia *quantos* estão parados em cada faixa de tempo, mas não **onde** travam.
Tabela nova, clicável (filtra a lista):

| Etapa | 7–14 | 14–30 | 30+ | Total |
|---|---|---|---|---|

Total de julho: 622 parados — 124 na faixa de 7–14 dias, 107 em 14–30 e **391 em 30+**.
A concentração no 30+ é o dado que a soma por faixa sozinha não deixava ver.

## Atualização — Bloco de indicadores (2026-08-13)

Card novo, **Esforço por resultado**, com três razões:

| Indicador | Conta |
|---|---|
| Clientes por proposta | clientes ÷ propostas |
| Visitas por proposta | visitas ÷ propostas |
| Propostas por venda | propostas ÷ vendas |

Quanto **menor**, melhor: é quantos passos foram precisos para produzir um do passo seguinte.
Cada tile mostra a conta embaixo (`109 clientes / 6 propostas`), para o número nunca aparecer
sem a base.

Segue o **recorte escolhido no funil** (61 · gerente · corretor) — os dois cards leem a mesma
linha, então trocar o recorte no funil move os indicadores junto. Não há chamada nova ao
servidor: tudo sai de `data.funil`.

Agosto/2026 até o dia 13, empresa: **18,2** clientes por proposta · **25,7** visitas por
proposta · **0,9** proposta por venda.

> ⚠️ **"Propostas por venda" ainda não é confiável.** Propostas Efetivas entrou em
> **agosto/2026** e as vendas vêm de `contratos`, que tem histórico completo — daí sair 0,9
> (menos propostas registradas do que vendas fechadas), o que é impossível na prática. O
> indicador só passa a valer quando o registro de proposta estiver em uso por todas as
> equipes. Em julho ele aparece como 0,0 pelo mesmo motivo (nenhuma proposta lançada).
>
> Os outros dois (clientes e visitas por proposta) sofrem do mesmo viés enquanto o
> denominador for pequeno.

Sem base, o indicador fica em **"—"**, não em zero: nenhuma proposta lançada não é eficiência
zero, é conta impossível.

## Atualização — Vazamento nos seletores de corretor (2026-08-13)

🐛 **Os seletores novos (funil, estoque) listavam corretor de outras equipes para o gerente.**

Auditoria com o gerente da SENNA, antes da correção:

```text
funil.corretores  : 39 corretores, equipes G61001 G61002 G61010 G61014 G61016 G61017 SENNA
estoque.corretores: 48 corretores, mesmas 7 equipes
```

Os números por equipe estavam certos (`funil.gerentes` e `estoque.gerentes` traziam só
SENNA), mas a **lista de corretores** era montada com um `query(Usuarios).filter(ativo)` sem
recorte — ou seja, o gerente podia escolher um corretor de outra equipe no dropdown e ver a
performance dele.

Causa: `teams` já chega filtrado quando o solicitante é gerente (`executive_view` corta a
lista antes), mas as duas funções construíam a lista de pessoas direto da tabela `usuarios`,
sem passar por `teams`.

Correção: `Usuarios.team.in_(ids_equipes)` nas duas. Depois:

```text
gerente SENNA : funil 1 gerente / 10 corretores · estoque 1 / 12 · parados 1 / 10  → só SENNA
admin         : funil 7 gerentes / 39 corretores · estoque 7 / 47 · parados 7 / 46 → tudo
```

`imoveis_parados.opcoes` **não tinha o problema**: ele é derivado dos itens, que já vêm da
query filtrada por equipe.

Também verificado que o recorte não é burlável: gerente da SENNA chamando com
`team=G61002` continua recebendo SENNA, e corretor recebe **403** na tela inteira.

## Atualização — Auditoria por equipe: equipes faltando (2026-08-13)

🐛 **Equipe sem visita e sem proposta no período sumia do bloco.** A linha só nascia quando
havia algo para contar, então a equipe parada — justamente a que precisa aparecer — ficava
invisível. Em agosto/2026 a **Alpha** não aparecia, com 7 equipes ativas e só 6 na auditoria.

Correção: o agrupamento passa a começar com **todas as equipes do escopo**, zeradas. Zero
visita é uma informação; ausência da linha é omissão.

```text
antes  agosto → 6 equipes na auditoria (faltava Alpha)
depois agosto → 7 equipes, Alpha com 0
depois julho  → 7 equipes, todas presentes
```

### Só equipe entra

As linhas `sem-equipe` e `administrativo` saíram. A auditoria é sobre o **acompanhamento do
gerente**: visita de corretor fora de equipe ativa não tem gerente para cobrar, então vira
ruído numa lista de pendências.

Ficaram de fora do bloco **inteiro**, inclusive dos totais — senão a soma das linhas não
fecharia com o cabeçalho. Conferido:

```text
agosto: cabeçalho 155 = soma das linhas 155   (KPI de visitas da página: 155)
julho : cabeçalho 490 = soma das linhas 490   (KPI de visitas da página: 494)
```

> **Os 4 de diferença em julho são de propósito** e é o único ponto de atenção: o KPI
> "Visitas" conta a operação toda; a auditoria conta só o que tem gerente responsável. Quem
> comparar os dois números vai achar 4 visitas a menos — de corretor sem equipe ativa.

### 🐛 Segundo defeito no mesmo bloco: teto de 500 visitas

A consulta trazia `limit(500)`. Julho já teve **494 visitas** — um passo do teto. Passando
dele, a auditoria começaria a **subnotificar pendência** (ou perder equipe inteira) sem
nenhum aviso, e o erro apareceria como "o gerente está em dia" quando não está. Teto subiu
para 5.000.

> Escopo confirmado depois das duas correções: gerente da SENNA continua vendo só a SENNA.

## Links

[[2.1 - Dashboards do Gerente]] · [[2.3 - Jornada de Captação]] ·
[[2.5 - Gestão de Clientes]] · [[3.7 - Permissionamento]] ·
[[1.1 - Coleta de Leads]] · [[3.5 - ETL de Vendas]]

---

## Atualização — Painel reformulado (2026-08-07)

Rodada de mudanças pedida pela diretoria. Tudo implementado e conferido contra a base de
produção; **deploy pendente**.

### 1. Acesso: gerente entrou na tela

A página deixou de ser exclusiva de `diretor`. Agora:

| Perfil | Enxerga |
|---|---|
| `diretor`, `administrativo` (permissão ou `team = administrativo`) | todas as equipes |
| `gerente` | **só a própria equipe** |
| demais | 403 / redirecionado |

A decisão é do servidor (`diretor_dashboard_routes._escopo`), a partir de `permissao`/`team` do
banco: para gerente, o parâmetro `?team=` da URL é **ignorado**. Se a equipe do gerente estiver
inativa, o escopo **não** cai para `None` — senão o fallback mostraria a empresa inteira.

Verificado:

```text
gerente G61015 sem parâmetro        → 200 · escopo=equipe · G61015 · 1 equipe na lista
gerente G61015 com ?team=G61002     → 200 · escopo=equipe · G61015   (parâmetro ignorado)
gerente G61015 com ?corretor=C61165 → corretor=None (é de outra equipe)
diretor ADM001                      → 200 · escopo=global · 7 equipes
corretor / id inexistente           → 403
```

No frontend o título vira **"Visão da Equipe"**, o seletor de equipe some e o nome da equipe
aparece fixo. A resposta traz `escopo: "equipe" | "global"`.

> ⚠️ **Risco conhecido (não corrigido):** `solicitante_id` é parâmetro de query, não vem do JWT.
> Quem tiver a `X-API-KEY` pode passar o id de outro usuário. Vale para esta e outras rotas —
> ver [[3.7 - Permissionamento]]. Com a entrada do gerente, o alcance da brecha aumentou.

### 2. Filtro de corretor

Sem equipe escolhida, o painel é por equipe (como antes). **Escolhida uma equipe, as linhas
passam a ser os corretores dela** e o segundo dropdown libera o filtro por corretor individual.
Trocar de equipe reseta o corretor.

O filtro propaga em visitas, funil, clientes, leads, captação, vendas, pendências e revisão.
Detalhes de fonte:

- **vendas por corretor** casam pelos 4 campos de nome do contrato (venda 1/2, captador 1/2) e
  **ignoram** o filtro de gerente — venda casada entre equipes conta para o corretor;
- **leads por corretor** casam `atendimento` por id **ou** nome: parte da base legada gravou o
  nome (a Renata tinha 80 leads assim, que apareciam como 0).

### 3. KPIs: 8 quadrados no lugar de 4

Ordem: **Leads C2S · Clientes · Visitas · Propostas · Vendas (quantidade) · VGV · VGC · % VGC/VGV**.

| KPI | Fonte / definição |
|---|---|
| Leads C2S | `leads_legado` no período (destino do importador Contact2Sale) |
| Clientes | clientes **distintos** atendidos em visitas do período, via `visita_cliente` — `clientes_visita` não tem data, a visita é a única âncora temporal |
| Visitas | `visitas` no período |
| Propostas | **propostas efetivas** ([[2.15 - Propostas Efetivas]]), não as SIM da visita |
| Vendas (quantidade) | contratos no período |
| VGV | soma de `valor_negocio` — soma simples; o rateio per-lado-cheio só vale no ranking por corretor |
| VGC | soma de `valor_total_61` (comissão que fica com a 61) |
| % VGC/VGV | razão dos dois; sem VGV mostra `—` |

Todos comparam com o período anterior. O back ainda devolve `propostas_visita_sim` para quem
quiser voltar o card às propostas SIM da visita.

Conferência (agosto 01–07): leads 475 · clientes 56 · visitas 80 · propostas 0 · vendas 3 ·
VGV R$ 5.150.000 · VGC R$ 170.000 · 3,3%.

### 4. Execução comercial: 3 métricas por linha

O card virou **Leads · Clientes · Visitas** por equipe (ou por corretor). Cada métrica tem
**escala própria**: a barra cheia é sempre quem lidera aquela métrica.

Dois descasamentos **reais**, sinalizados no rodapé do card:

- **leads somam menos que o KPI** — leads da recepção chegam com equipe `61 Imóveis`, sem equipe
  atribuída (88 de 475 em agosto);
- **clientes somam mais que o KPI** — cliente que visitou com corretores de equipes diferentes
  conta uma vez em cada equipe, e uma só no total.

### 5. Card "Visitas" (era "Funil de visitas")

Renomeado. A barrinha abaixo da pizza virou **quantitativa**: a largura é o volume de visitas
classificadas, não mais 100% para todos. A composição SIM/TALVEZ/NÃO continua dentro da barra.

Maio: AGUIA 127 (100%), LOTUS 107 (84%), SENNA 106 (84%), NOVA UNIÃO 86 (68%), LIDER 41 (32%),
AGEF 9 (7%), Alpha 6 (5%). Antes AGEF e AGUIA tinham a mesma largura.

### 6. Jornada de captação: duas leituras por etapa

O card "Movimentações de etapa" virou **"Etapas no período"**, com dois números:

- **Entraram** — avanços em `captacao_historico` no período, contando **captação distinta**
  (antes contava evento: quem avançasse duas vezes contava dobrado);
- **No período** — quem esteve na etapa em algum dia do intervalo, do snapshot diário
  (`captacao_snapshot`), somando quem **já estava** antes.

Agosto (01–07): Prospecção 204 entraram / 458 já estavam / **662 no período**.

> Depende do snapshot diário estar rodando — ver [[2.4 - Snapshot e Evolução de Captação]].
> Proteção: "no período" nunca fica menor que "entraram".

### 7. Fechamentos: tabela com 9 colunas

**Endereço · Equipe · Corretor · Valor · Data · Valor Total 61 · % compra · Metragem² · Valor m²**

- **Endereço** = campo `contrato` (o nome do contrato **é** o endereço: "SQS 307 Bloco F Apartamento 401");
- **Equipe** sai do corretor da venda (cadastro), caindo no gerente do contrato como plano B;
- **% compra** = `valor_total_61 ÷ valor_negocio` (comissão **efetiva**). O campo
  `percentual_comissao_61` foi descartado porque diverge do realizado (ex.: combinado 5%,
  efetivo 3,5%);
- **Metragem²/Valor m²** — ver §8.

### 8. Metragem: por que precisou de um cache

A premissa "buscar a metragem pelo código do imóvel" **não se sustenta para venda fechada**: a
API do Imoview só devolve imóvel **ativo**. Testado:

- `RetornarImoveisDisponiveis` por endereço → 0 resultados para imóvel vendido;
- endpoints de imóvel único (`RetornarDadosImovel`, `RetornarImovel`, `ObterImovel`) → 404;
- filtro por `codigo`/`codigos`/`codigoimovel` → ignorado;
- catálogo completo via `POST /Imovel/RetornarImoveis` (740 imóveis, 38 páginas) → **nenhum
  código vendido presente**.

Também não há metragem em nenhuma tabela local indexada por código do Imoview.

**Solução:** tabela `imovel_area` + job `sync_areas_imoview.py`, que varre o catálogo e grava a
área **antes** de o imóvel vender. Cobre daí em diante; venda antiga cujo imóvel já saiu do ar
fica sem metragem (1 de 65 contratos de 2026 tinha área no primeiro sync).

Dois defeitos corrigidos no job: as áreas vêm como **texto BR** (`"95,00"`) e `areaprivativa`
é **booleano** no mesmo payload — um `True` estava virando "1 m²".

> 🐛 **Bug de origem, não corrigido:** `contratos.codigo_imovel` é gravado como número formatado
> (`"10.961,00"` em vez de `10961`), o que zera qualquer cruzamento por código. Há normalização
> no consumo (`_codigo_imovel`), mas o **importador de contratos continua gravando errado**.

### 9. Contratos em atraso (Governança)

Bloco novo com 3 contadores + lista dos piores casos. Como a base **não tem marcação de
concluído/pago**, "atraso" é necessariamente prazo vencido:

- prazo da comissão (`parcelas_comissao`) já passou;
- contrato fechado há mais de 30 dias **sem** `data_assinatura`.

Janela de **12 meses**: sem ela o contador dava **1.102**, porque contratos de 2015–2024 não têm
data de assinatura preenchida ("3.895 dias sem assinatura") — é base histórica incompleta, não
backlog. Com a janela: 25 (7 comissão + 18 assinatura).

Quitação/posse previstas vencidas foram **descartadas de propósito** (145 e 140 contratos): sem
marcação de conclusão, quase todo contrato antigo cairia nelas.

### 10. Achado não corrigido

**VGV/VGC por equipe podem vir zerados.** O filtro de vendas por equipe depende de
`gerente_venda_nome` bater com o gerente cadastrado, e para várias equipes não bate — a AGUIA em
maio mostra VGV 0 mesmo com 85 visitas e 518 leads, enquanto uma corretora dela sozinha tem
R$ 1,7M no período. Corrigir exigiria casar o contrato pela equipe dos corretores envolvidos.

---

> **Do agregado para o item.** Esta tela responde "como está a operação";
para descer até *um* imóvel — quem captou, se é foco, mídia, visitas, propostas e venda —
use a [[2.16 - Consulta de Imóveis]]. O estoque exibido aqui e o catálogo de lá saem da
mesma fonte (`imovel_area` + API do Imoview), então os números batem.

---

## Atualização — Visitas sem equipe sumiam do total (2026-08-17)

**Sintoma:** julho mostrava **490** visitas aqui e **598** na [[2.5 - Gestão de Clientes]].
O total real é **494**.

**Causa.** `_visit_metrics` fazia `INNER JOIN` com `usuarios` e só contava a visita se o
`team` estivesse entre as equipes **ativas**. O total do painel é
`sum(row["visitas"] for row in team_rows)` — visita que não cai em nenhuma linha de equipe
simplesmente evaporava:

```
usuário SEM team          diretor         1
equipe INATIVA            administrador   3
ok                        corretor      469
ok                        gerente        21   <- contadas (intencional)
                                        ---
                                        494
```

**Correções**
- `join` → `outerjoin`: visita cujo `id_corretor` não existe mais em `usuarios` deixa de ser
  descartada.
- As órfãs (sem `team`, equipe inativa ou não cadastrada) passam a somar numa linha
  **"Sem equipe"**. Agora a soma das linhas **é** o total real.
- A linha só aparece quando não há equipe filtrada, e entra apenas em `equipes` (a tabela) —
  **não** em `equipes_opcoes`, que alimenta o dropdown de filtro. O filtro por equipe fica intacto.

```
LIDER 123 · AGUIA 103 · NOVA UNIÃO 91 · LOTUS 68
SENNA  61 · AGEF   38 · Alpha        6 · ** Sem equipe ** 4
soma = 494
```

Note que o painel **conta as 21 visitas lançadas por gerentes** — é intencional e já havia
comentário no código dizendo isso (`_team_brokers`). Era uma das diferenças em relação à
Gestão de Clientes, que filtrava `permissao='corretor'`.

Detalhe da investigação em [[_Registro - 2026-08-17]] §3.
